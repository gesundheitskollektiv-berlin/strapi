'use strict';

/**
 * geko-page-angebote controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::geko-page-angebote.geko-page-angebote');
